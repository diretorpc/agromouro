# Railway — Root Directory deve ser configurado como /api no painel
# Nixpacks detecta Node.js pelo package.json e usa estes comandos

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
